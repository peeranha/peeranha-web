// import { fromJS } from 'immutable';
// import { selectSuggestedCommunitiesDomain } from '../selectors';

describe('selectSuggestedCommunitiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
