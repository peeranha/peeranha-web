// import { fromJS } from 'immutable';
// import { selectChangePasswordByPreviousDomain } from '../selectors';

describe('selectChangePasswordByPreviousDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
