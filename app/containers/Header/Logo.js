import styled from 'styled-components';

const Logo = styled.div`
  display: flex;
  flex-basis: 210px;
  justify-content: left;

  img {
    width: 180px;
    position: relative;
    height: 40px;
    top: 5px;
    cursor: pointer;
  }
`;

export default Logo;
