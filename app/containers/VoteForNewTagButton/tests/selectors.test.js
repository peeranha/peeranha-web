// import { fromJS } from 'immutable';
// import { selectVoteForNewTagButtonDomain } from '../selectors';

describe('selectVoteForNewTagButtonDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
