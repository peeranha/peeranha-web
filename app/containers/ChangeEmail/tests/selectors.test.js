// import { fromJS } from 'immutable';
// import { selectChangeEmailDomain } from '../selectors';

describe('selectChangeEmailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
