/*
 * UserProfileNav Messages
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  profile: {
    id: 'app.containers.UserProfileNav.profile',
    defaultMessage: 'Profile',
  },
});
