// import { fromJS } from 'immutable';
// import { selectViewProfilePageDomain } from '../selectors';

describe('selectViewProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
