// import { fromJS } from 'immutable';
// import { selectShowOwnerKeyDomain } from '../selectors';

describe('selectShowOwnerKeyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
