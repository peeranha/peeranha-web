// import { fromJS } from 'immutable';
// import { selectFollowCommunityButtonDomain } from '../selectors';

describe('selectFollowCommunityButtonDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
