/*
 * AnswerForm Messages
 *
 * This contains all the text for the AnswerForm component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  titleLabel: {
    id: 'app.containers.AnswerForm.titleLabel',
  },
});
