import { Link } from 'react-router-dom';
import styled from 'styled-components';

const A = styled(Link)`
  text-decoration: none;
  color: #282828;

  :visited {
    text-decoration: none;
  }

  :hover {
    text-decoration: none;
  }
`;

export default A;
