import styled from 'styled-components';
import { white, gray } from 'style-constants';

const UserNavigationStyled = styled.header`
  border-top-left-radius: 5px;
  border-top-right-radius: 5px;
  margin-bottom: 0;
  padding: 24px 32px;
  background-color: ${white};
  border-bottom: 1px solid ${gray};
`;

export default UserNavigationStyled;
